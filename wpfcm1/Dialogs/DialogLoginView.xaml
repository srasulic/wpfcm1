<mah:MetroWindow x:Class="wpfcm1.Dialogs.DialogLoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    ShowCloseButton="False" ShowMinButton="False" ShowMaxRestoreButton="False"
    WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
    BorderBrush="{DynamicResource AccentColorBrush}"
    BorderThickness="1"
    Loaded="DialogLoginView_OnLoaded"
    Width="500" Height="290"
    Topmost="True"
    FontSize="14"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Varijacija:" VerticalAlignment="Center" />

        <Label Grid.Row="1" Grid.Column="0" Content="Korisničko ime:" VerticalAlignment="Center" />

        <Label Grid.Row="2" Grid.Column="0" Content="Lozinka:" VerticalAlignment="Center"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Firma:" VerticalAlignment="Center"/>


        <ComboBox x:Name="Variations" Grid.Row="0" Grid.Column="1" Margin="8" VerticalAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="Name" />
                                <Binding Path="ApiUrl" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBox x:Name="UserName"
            Grid.Row="1" Grid.Column="1"  Margin="8" VerticalAlignment="Center"
        />

        <PasswordBox x:Name="Password" PasswordChanged="PasswordBox_PasswordChanged"
            Grid.Row="2" Grid.Column="1" Margin="8" VerticalAlignment="Center"
        />

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button Content="Login" x:Name="OnLogin" MinWidth="64" Margin="8"/>
        </StackPanel>

        <ComboBox x:Name="Tenants"
            Grid.Row="4" Grid.Column="1" Margin="8" VerticalAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="naziv" />
                                <Binding Path="tenant" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>


        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="OK" x:Name="OnClose" MinWidth="64" Margin="8" />
            <Button Content="Cancel" x:Name="OnCancel" MinWidth="64" Margin="8"/>
        </StackPanel>

    </Grid>
</mah:MetroWindow>
