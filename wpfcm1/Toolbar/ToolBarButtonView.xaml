<UserControl x:Class="wpfcm1.Toolbar.ToolBarButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="b2vc"/>
    </UserControl.Resources>
    <Button Style="{DynamicResource MetroCircleButtonStyle}" Width="55" Height="55" x:Name="SendMessage" ToolTip="{Binding Description}"
            Visibility="{Binding Path=ButtonVisibility, Converter={StaticResource b2vc}}">
        <Rectangle Width="21" Height="21"
                   Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
            <Rectangle.OpacityMask>
                <!--<VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_home_empty}" />-->
                <VisualBrush Stretch="Fill" Visual="{Binding Path=ButtonImg}" />
            </Rectangle.OpacityMask>
        </Rectangle>
    </Button>
</UserControl>
