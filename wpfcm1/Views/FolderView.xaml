<UserControl x:Class="wpfcm1.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ListView x:Name="Documents">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Path=DocumentInfo.Name}" Width="210"/>
                    <GridViewColumn Header="Date modified" DisplayMemberBinding="{Binding Path=DocumentInfo.LastWriteTime}" Width="130"/>
                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding Path=DocumentInfo.Length}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
